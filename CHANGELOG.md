<div align="center">

# 💌 更新日志

记录“愿此朝夕陪伴你的生活”的每一次正式发布。

[使用说明](./README.md) · [版本发布](https://github.com/siciyuanweilai/astrbot_plugin_daily_life/releases)

</div>

## 🌸 v1.1.0 · 2026-08-04

> 💗 把日程中的内部生活动作落成可查询、可结算、可迁移的生活实况，同时保留外部行为的真实回执边界。

### 🧭 生活动作与统一时间线

- 新增活动会话，显式计划动作会记录为待进行、进行中、已完成、失败、取消或过期，并与原有动作回执、状态结算和决策轨迹保持同一条主链。
- 休息、用餐、做饭、采购、家务、运动和出行等纯虚拟内部动作可在时间轴完成后生成“模拟回执”；聊天、社交、换装、拍照、发图和视频仍必须等待用户、工具或媒体产物等真实回执。
- Dashboard 新增“生活实况”，统一展示活动、饮食库存、家务轮换、运动负荷和会话行动项；设置页中的“生活实况”紧随“生活背景”展示，并按基础与结算、地点与出行、生活记录、行动项与上下文分组，真实与模拟来源继续分开保存。

### 🍚 饮食、家务、运动与出行

- 新增食谱、食材库存、库存流水和用餐记录；采购增加库存，做饭按结构化食材清单扣减，库存不足时拒绝动作并留下局部重排依据。
- 新增家务周期、上次完成和下次应做时间；运动保存时长、强度和负荷分数，后续日程与聊天按固定预算读取近期记录。
- 地点和出行支持高德地图、腾讯地图与百度地图，可在设置中选择服务商并配置对应的服务端 Key；常住详细地址成为唯一居住位置来源，解析出的城市同时用于天气、地点搜索和公交路线，“家”的坐标自动缓存。
- 三家地图统一提供地址解析、地点搜索、输入提示、地点详情和步行、骑行、驾车、公交路线；百度 BD-09 坐标在适配层转换为插件统一坐标，切换服务商不会混用接口或悄悄回退到另一家地图。
- 新增地点搜索、路线比较、地点详情和限时出行规划四个自然语言工具；当前服务商未配置 Key 时自动隐藏这些工具，返回模型的结果不包含地点坐标。网络、Key 或地址解析不可用时明确保持不可用，路线计算仍可在已有坐标条件下使用本地估算。

### 💬 行动项、表达与兼容性

- 已确认的未来承诺同步为带负责人、截止时间和状态的会话行动项；完成、取消、过期、改期和纳入日程会同步更新，不新增关键词或正则识别链。
- 日程和普通聊天按稳定优先级与字符预算注入生活实况，不根据特定词语临时触发。
- 模型语义分段超时或返回无效时，自然分段会按强标点分组评估局部长句，并为后续句号、问号等强边界预留段数；“语义分段数量”继续作为上限，不会要求短回复强行凑满。
- “记忆抽屉 → 世界 → 裁定”按会话响应、记忆处理、主动互动、表达方式和流程状态细分；每条记录显示稳定的类别、来源、阶段与结果，主动回复和私聊回访不再使用动态拼接动作名落库。

### ⬆️ 升级与数据兼容

- 数据库升级为 v6：v5 领域表和地点坐标结构继续保留，并为动作裁定增量增加类别、来源、阶段和结果字段。旧记录仅按已有动作枚举与主动裁定协议结构回填，不读取或改写理由正文，无需清空或手动重新分类。
- 高德地图仍是默认服务商；腾讯地图和百度地图为可选通道，只有主动选择后才需要配置对应的服务端 Key。

## 🌸 v1.0.9 · 2026-08-03

> 💗 收敛媒体准备、外部地址和后台任务边界，减少重复下载与并发写入造成的失败。

### 🎬 视频与音频

- 聊天视频理解和 B 站专业总结共享同一份物理素材准备结果；不同总结用途只在准备完成后分别生成，不再因为用途不同重复下载。
- 已准备的视频在必剪转写模式下直接本地提取音频，不再为同一视频再次调用 `yt-dlp`；音频和视频缓存均使用临时文件完成后原子替换，避免并发读取半成品。
- 共享准备键不绑定会话范围，同一视频被不同入口或会话同时引用时也能复用；AstrBot 关闭时会同时取消总结任务和素材准备任务。

### 🔒 外部输入与任务可靠性

- 视频直链、表情素材和角色/好友参考图统一拒绝本地协议、回环地址、内网地址、链路本地地址和明显的云元数据地址；域名请求前增加 DNS 解析结果检查。
- 后台任务面板状态增加各类别的丢弃、完成和失败计数；聊天记忆进入队列失败会明确写入告警，不再静默丢失。
- 数据库仍为 v4，不新增无必要的分类表，也不要求清空或重建旧数据。

### 💬 表达与维护

- 极短的单句回应不再为无法拆分的内容额外等待一次模型调用，较长或多动作回复仍走模型语义分段和自然分段兜底。
- 新增媒体共享准备、跨会话复用和外部地址安全回归测试。

## 🌸 v1.0.8 · 2026-08-02

> 💗 修复首次启动日程生成早于消息平台连接的竞态，确保首轮生活背景使用可查询的联系人和历史记录。

### 🚦 启动与平台就绪

- 首次生活初始化在生成当天日程前等待已配置的平台适配器真正就绪；不再把“平台实例已创建”误认为已经连接。
- OneBot/aiocqhttp 会等待反向 WebSocket 客户端建立后再执行联系人昵称、群名和历史记录查询，避免启动日志中出现“适配器尚未连接但日程已生成”。
- 无平台、仅网页聊天或测试替身会直接放行；真实平台连接异常时使用 120 秒超时保护并记录降级日志，不会无限阻塞插件启动。
- 保留现有数据库结构 v4，无需手动迁移数据库。

## 🌸 v1.0.7 · 2026-08-02

> 💗 把生活决策、节律趋势、研究任务和聊天分段的边界补齐，让后台状态与实际投递结果保持一致。

### 🧭 生活决策与趋势

- 修复穿搭更新在保存决策前引用不存在局部变量的问题；保持原穿搭也会留下可追溯的决策证据，不再被上层误报为更新失败。
- 修复同一业务日同时存在短期和持续生理节律时的聚合错误，趋势会分别保留各生命周期的最新有效证据。

### 🌐 研究任务与工具边界

- 研究报告任务成功时收束为 `completed`，失败、取消或超时收束为 `failed`；AstrBot 重启后不会把错误报告误恢复成成功任务。
- 网站地图、整站抓取、研究报告仍使用 Tavily，并继续执行公网地址校验、会话隔离、结果限长和持久任务恢复。
- QQ 专属文本转发继续只面向 OneBot/QQ；非 QQ 平台不会误走合并转发路径。

### 💬 表达与兼容性

- 保留模型语义分段；模型返回结构合法但把多个完整句合并成一段时，才使用通用自然停顿兜底。
- 修复分段消息已经投递但后处理失败时返回“未发送”的问题，避免上层重复发送。
- 数据库结构保持 v4；旧数据库仍由启动迁移流程按事务升级，无需手工分类、清空或重建历史数据。

## 🌸 v1.0.6 · 2026-08-02

> 💗 让状态、动作、记忆和数据清理只保存真正能影响后续生活的内容。

### 🧭 状态与动作闭环

- 后台状态刷新只更新当前状态和日程可行性，不再把模型临时推测自动沉淀为长期偏好或生活事件。
- 计划动作到点后不会自动生成“已执行”回执；没有用户、工具或媒体结果等可验证证据时标记为“已过期”，只有明确回执才会结算状态和时间事实。
- 穿搭保持不变时不再重复写入生活决策，减少穿搭记录挤占日程、关系和承诺记录。
- 当前地点和穿搭继续作为时间事实保存，状态数值不再重复复制到时间事实表。

### 🧠 记忆与节律

- 记忆召回默认使用结构化查询和向量排序；模型语义排序改为显式高级开关。
- 普通聊天效果学习增加独立开关，默认关闭；主动回复的成功投递和后续反馈仍可正常记录。
- 生理节律趋势改为每个日期取一条代表记录，避免最新一天的高频刷新冒充多日趋势。
- 认知运行新增保留天数和实际清理逻辑，当前有效事实不会被清理，已结束任务、轨迹、回执和历史情绪按生命周期回收。
- 全局时间事实在私聊或群聊上下文中可被正确读取，减少“已经记录但聊天看不到”的情况。

### 🌐 外部工具闭环

- 网站地图、整站抓取和研究报告工具继续使用 Tavily，并统一限制为公网 HTTP(S) 地址、深度/广度/页面数量上限和重复页面去重。
- 研究报告任务写入现有可恢复任务队列；AstrBot 重启后会恢复未完成任务的后台轮询，完成或失败结果会收束到原任务记录。
- 研究任务绑定创建它的会话，其他会话不能通过任务编号读取结果；模型语义分段功能和自然分段兜底保持不变。
- QQ 专属文本转发仍只在 OneBot/QQ 可用，原文按会话隔离、限时缓存并通过合并转发发送，其他平台明确返回不支持。

### 🖥️ 工具与前台

- 未启用的图片、视频和语音工具不再进入当前模型工具列表；视频理解、B站自动总结和自动表情收集默认关闭，明确使用时仍可调用。
- Dashboard 默认展示今天、状态、关系、承诺和生活事件；决策轨迹、任务租约、伪聚类和内部节律记录收进诊断层。
- 本地 ASR 安装完成后清理下载副本，避免同一模型重复占用磁盘。

## 🌸 v1.0.5 · 2026-08-01

> 💗 让生活动作从“到点推演”走向“回执确认、事实结算、失败重排与重启恢复”的完整闭环。

### 🕰️ 动作回执与生活事实

- 新增生活动作执行回执，区分已确认、时间轴模拟、失败和取消；图片、套图、视频等真实工具结果会回写对应计划动作，时间轴到点完成则明确保存为模拟证据，不再混同为外部执行成功。
- 动作结算与实时状态刷新会把当前地点、穿搭和数值状态同步为带有效期、来源和替代链的时间事实；聊天或社交动作有明确对象时，也会保存最近互动事实。
- 失败或取消回执会收束对应时间轴项目，保存失败原因和待重排依据；下一轮状态巡检只能替换尚未开始的未来锚点，不能改写已开始、完成或无关的日程。
- 运行时反思正式接入十二小时冷却；达到评分和证据门槛但仍在冷却期时不会重复生成反思。

### 🎨 媒体投递与兼容性

- 单图、参考图编辑、套图和视频在取得产物后、发送前登记持久投递任务；正常送达后立即收束，插件若在两者之间重启，会恢复投递并补交动作回执。
- 恢复任务只发送已经生成的本地文件或结果地址，不会重新调用第三方图片、视频生成接口，避免重启造成重复生成、重复计费或画面漂移。
- 修复视频抽帧、音频提取和 yt-dlp 的 FFmpeg 备用路径缓存；系统 FFmpeg 不可用或运行环境变化时，会重新探测 `imageio-ffmpeg` 提供的可执行文件。

## 🌸 v1.0.4 · 2026-08-01

> 💗 将生活生成扩展为可验证、可恢复、可回放的认知与行动闭环。

### 🧠 认知、证据与长期成长

- 新增时间化事实存储，使用 `ADD / UPDATE / INVALIDATE / NONE` 显式维护有效期、替代版本、来源和证据信号；历史事实保留，但过期版本不会继续作为当前事实注入聊天。
- 新增有门槛的反思机制；只有重要度、证据数量和结构化断言完整时，反思才会晋升为可复用人格认识。
- 聊天记忆批次可直接产出时间事实操作，并锁定真实会话、消息编号和时间，不允许模型伪造其他作用域的来源。
- 隐藏上下文增加当前时间事实、已晋升人格、三层情绪和有证据日记，同时明确情绪不能覆盖人物、消息、图片、日程或工具事实。

### 🕰️ 行动、计划与恢复

- 日程增加 4 至 6 个稳定锚点和未来 3 小时近程细化；局部重排只能调整尚未开始的未来锚点，已开始或完成的活动不会被回写。
- 每个生活动作使用显式动作类型、前置条件、效果和结算状态；确定性代码验证条件后才允许修改白名单状态字段，并保留动作结果与统一决策轨迹。
- 每日刷新、夜间复盘、私聊回访和闲时主动检查接入持久任务队列，支持幂等键、租约、重试上限、失败原因和启动恢复；任务载荷不包含也不会执行任意代码。

### 💞 情绪、关系与主动互动

- 新增短时、日级和关系级三层情绪，按不同半衰期衰减；关系状态必须引用已落库证据，单次变化受到小幅边界限制。
- 主动互动增加候选、考虑、等待、发送、互动、收束、冷却、中断和放弃生命周期，普通消息到来可以中断过期候选。
- 主动发送增加收益、时机、连续性、打扰和不确定性五项评分；确定性净收益未达门槛时继续观察，实际发送成功后才提交情节记忆和最终决策轨迹。
- 夜间日记采用第一人称，但只能引用真实事件、决策、反馈和回复效果记录；无法验证证据时不保存日记或关系变化。

### 🖥️ 面板、回放与升级

- 仪表盘新增时间事实、人格断言、候选反思、三层情绪、生活动作结算、决策轨迹、有证据日记和可恢复任务状态，结构化状态使用中文显示。
- 新增离线场景回放器和八类默认场景，统计决策准确率、无依据断言率、生命周期完整性、状态连续性与主动打扰风险，便于后续改动做确定性回归。
- 数据库结构升级到 v3：新增认知、任务、动作、情绪和日记表，并为旧情绪记录补齐层级、基线与衰减字段。插件重启时自动按事务执行 `v2 -> v3`，旧表和旧记录保留，失败会回滚。
- 数据库迁移只做结构增量和明确字段补齐，不从聊天摘要等自由文本反推时间事实、人格断言或日记；新认知表在产生新证据前为空是正常现象。

## 🌸 v1.0.3 · 2026-08-01

> 💗 重点补齐首次启动与自主生活执行闭环，并减少稳定生活阶段的重复模型调用。

### 🕰️ 自主生活闭环

- 首次安装时，插件启动会在后台自动补全当前业务日，不必等到第二天定时任务或第一次聊天才生成日程；生成期间仍使用防编造约束。
- 时间轴新增待进行、进行中、已完成、已跳过和已取消执行状态，并记录状态变化原因、依据和更新时间；面板会直接显示每项活动的当前执行状态。
- 后台巡检会依据权威时间轴时钟推进活动，夜间复盘再结合当天实际状态、互动和事件收束结果，不通过关键词猜测活动是否完成。
- 状态巡检在生活稳定时按基础间隔逐步退避，最长 90 分钟；到达新日程节点时会及时恢复检查，减少连续“保持不变”的模型调用。
- 穿搭判断与状态刷新解耦，只在当前/下一项日程、时段、天气温度区间、外出意愿或睡眠状态发生变化时重新调用模型；普通轮询不再重复判断同一套穿搭。
- 数据库结构升级到 v2，已有时间轴自动补为待进行状态，无需手动迁移或清空历史数据。

### 🖥️ 前台与可读性

- 补齐今日状态、世界记录、生活演化、体验层和表情口袋使用的来源、动作、状态与类型中文映射；未识别的结构化英文值统一使用中文安全回退，不再直接暴露内部标识。
- 自由叙述、文件名以及 MemOS、Grok、模型名称和接口字段等必要技术名称保留原文，不通过关键词替换改写模型内容。

## 🌸 v1.0.2 · 2026-07-31

> 💗 重点修正生图通道配置在桌面端和移动端的响应式布局，并统一新关系与记忆记录的叙述视角。

### 💞 关系与记忆

- 关系档案、会话摘要、群聊环境、消息留意、动作裁定、行为反馈和场景词等新记录，统一从当前角色自身视角生成；当前角色参与的动作、感受、关系和互动使用第一人称，其他人物仍按证据中的身份称呼。
- 聊天记忆批次现在直接复用人物称谓与叙述视角规则，根据语义主体决定视角，不通过前端正则、关键词表或固定词面替换推断主体。
- 面板保持展示模型保存的原文，不对历史自由文本做猜测式改写；升级前已有记录继续保留原文，重新生成或后续新增的记录才使用新规则。

### 🖥️ 仪表盘与配置

- 生图通道在移动端改为两层响应式布局：拖动和删除操作独立成行，接口类型与分组名称并排，地址、密钥和模型使用整行，尺寸、比例和超时稳定三列显示，避免标签、输入框和选择器重叠。
- 桌面端的图片尺寸与图片比例下拉菜单使用独立宽度并向右对齐，不再被紧凑输入框挤窄；菜单仅纵向滚动，不再出现底部横向滚动条。
- 为通道字段增加结构化字段标识，使响应式布局按字段职责排布，不依赖显示文案或字段顺序。

## 🌸 v1.0.1 · 2026-07-30

> 💗 重点修正聊天与生活事实脱节、视频素材重复准备和生图通道难以辨认的问题，并让穿搭生图与配置面板更符合连续生活场景。

### 💬 聊天表达与私聊回访

- 优化自然分段兜底：短反应或短完整句后接较长说明时，可以在强标点处分开；软停顿会结合目标长度选择更平衡的切点，减少整句不分或碎片化分段。
- 最近私聊上下文增加时间和媒体标记，并扩大可参考消息范围；旧照片、旧回复和未来约定不会再被误写成刚刚发生或已经完成。
- 私聊回访现在与闲时回复共用私聊静默门槛，未达到 `proactive_config.private_idle_minutes` 时直接等待，不提前调用模型。
- 回访生成会核对当前活动、身心状态、日程窗口和未完成约定；候选文本缺少事实依据或与当前时间线冲突时取消发送。
- 回访生成期间出现新消息，或发送前会话、日程与生活状态已经变化时，会丢弃旧回复并把普通聊天置于优先位置。
- 明确旧聊天文本不是当前事实凭证；当旧回复与结构化生活状态、真实工具结果或时间线冲突时，以可验证事实为准。

### 👗 穿搭与图片生成

- 区分“真实换装”和“只生成试穿效果图”：真实换装会先更新当前业务日的穿搭状态，再使用同一套已保存造型生图；普通创作或试穿不会改写生活状态。
- 真实换装没有产生有效的新造型、状态更新失败或与反推提示词冲突时会取消生图，避免连续重复换装或图片与面板穿搭不一致。
- 文生图和图生图通道新增自定义分组名称；图片请求、完成、尺寸异常和失败日志都会显示实际通道名称，便于区分主备接口。
- 生图接口面板改为单行紧凑布局，缩短地址、密钥、尺寸、比例和超时等控件占位，并移除横向滚动条。
- 简化当前角色与好友参考图说明，明确参考数量和建议视角。

### 🎬 视频理解

- 视频总结先准备一次视频素材，再让本地 ASR 或必剪转写复用该文件或从中提取音轨，避免同一 B 站或远程视频分别重复下载视频与音频。
- 本地准备结果无法转写时仍会回退到原始来源，保留原有容错能力。
- B 站视频缓存改用 `BV 号 + cid` 作为稳定身份，避免签名直链变化导致同一视频重复缓存。

### 🖥️ 配置与兼容性

- 现有配置和数据库无需手动迁移；旧生图通道未填写分组名称时会继续使用自动生成的通道名称。
- `proactive_config.private_idle_minutes` 现在同时约束私聊闲时回复和私聊回访，升级后回访可能比旧版本更晚触发。
- 新增并更新聊天分段、私聊回访、真实换装、视频素材复用、生图通道和仪表盘布局回归测试。

## 🌱 v1.0.0 · 2026-07-29

> 💖 首次正式发布。愿此朝夕陪伴你的生活，让角色不只是在消息里出现，而是拥有可以延续的作息、心绪、关系与片刻。

### ✨ 发布亮点

- 🕰️ 让日程、状态、睡眠、天气、穿搭和生活事件在每天的朝夕间连续演化。
- 💞 让关系记忆、随心回复、闲时续话和聊天表达共同塑造更自然的陪伴。
- 🎨 将图片、套图、合影、视频、语音、表情素材和专业视频笔记接入同一套生活上下文。
- 🌐 通过 Tavily 网页能力、Grok X 搜索、MemOS 与本地记忆补足现实信息和长期延续。
- 🖥️ 提供可视化仪表盘、配置自动保存、数据库迁移、缓存复用与后台任务生命周期管理。

### 🕰️ 自主生活

- 新增每日生活背景：主题、心情色彩、天气、状态、日程、穿搭、时间轴与生活事件会共同生成并持续演化。
- 新增业务日边界与周计划。凌晨可以自然延续前一晚，周主题和日计划作为生活判断的软参考，而不是硬性剧本。
- 新增状态、注意力与睡眠层：体力、忙碌度、社交意愿、睡眠债、无聊值、摸鱼值和观看状态会影响活动强度、邀约与回复意愿。
- 新增穿搭连续性判断：地点变化不等于立刻换衣；外出、居家、洗澡、睡前与后续计划共同决定保持、局部调整或完整换装。
- 新增日终复盘、偏好学习、生活事件与计划约定，让之后的生活从已有经验继续，而不是每天重置。

### 💞 关系与聊天

- 新增关系档案、聊天摘要、长期偏好、主观印象和人物事实边界；称呼、性别与亲疏只使用明确资料，不根据昵称或语气猜测。
- 新增随心回复、闲时回复和可选私聊回访：即时消息先裁定回应、观察或等待；会话安静后才决定是否自然续话。
- 新增聊天表达层：模型可按完整表达动作分段；失败时回退到通用换行、标点与长度规则，不依赖关键词或固定句型。
- 新增消息撤回处理：撤回的用户消息会移出当前上下文与主动回复候选，避免后续继续引用已撤回内容。
- 新增回复效果学习：只有观察到真实后续反馈时才学习表达偏好，不猜测用户态度。

### 🎨 图片、视频与语音

- 新增 GPT Image 2、Gemini 图片生成，支持文生图、图生图、角色参考图、好友参考图、生活套图与合影。
- 新增人物身份与造型归属：当前角色和好友分别维护外观、穿搭与发型，降低合影中的串脸、服装串用和身份互换。
- 新增 Grok 视频生成与图片首帧策略；新拍、续拍、合影视频和失败兜底按不同语义处理。
- 新增 B 站与普通视频理解：音频和视频素材并发准备，关键画面按变化筛选并优先多图理解。
- 新增专业视频笔记：按实际议题组织背景、论点、事实、分析、风险与建议；广告和无关片段不渲染，长内容按连续时间窗稳定合并。
- 新增 SiliconFlow 情绪语音、智能语音切换、工具状态表情和表情素材识别。

### 🌐 搜索与外部记忆

- 新增 Tavily 网页 Search、Extract、Map、Crawl 与 Research；Grok 只负责 X 平台原生搜索，网页与 X 来源明确分层。
- 新增 Quick/Deep 搜索策略、日期与域名限制、图片搜索、证据质量判断、网址定向回退和后台 Research 轮询。
- 新增搜索缓存与并发合并：相同搜索或网页读取只执行一次上游请求；缓存键隔离深度、日期、来源、域名及图片参数，失败结果不写入缓存。
- 新增 MemOS 外部长期记忆：聊天前短超时检索，超时或不可用时不阻塞主回复；可异步同步精选摘要、偏好、备忘录与纠错。

### 🖥️ 仪表盘与配置

- 新增仪表盘：查看今日生活、当前状态、时间轴、关系与世界记忆、生活演化、体验层和多模态资料。
- 新增设置分组与自动保存，可管理模型、闲时回复、搜索、MemOS、图片、视频、语音与参考图。
- 新增 QQ/OneBot 与个人微信平台适配，并为平台昵称、群聊名称和参考对象提供稳定的解析与缓存边界。

### 🛡️ 稳定性与维护

- 后台任务、外部调用与媒体生成具备生命周期保护；插件重载或终止时会取消或收束未完成工作，避免阻塞 AstrBot 主事件循环。
- 高频模型提示词统一采用稳定规则前置、动态资料后置的结构；人物、搜索、MemOS 与联系人缓存增加等价键规范化、容量限制和失败重试边界。
- 完整回归、静态检查和编译检查均已通过。
