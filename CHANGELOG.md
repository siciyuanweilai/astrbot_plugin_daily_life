<div align="center">

# 💌 更新日志

记录“愿此朝夕陪伴你的生活”的每一次正式发布。

[使用说明](./README.md) · [版本发布](https://github.com/siciyuanweilai/astrbot_plugin_daily_life/releases)

</div>

## 🌸 v1.2.8 · 2026-08-17

> 💗 让视觉衣橱真正参与日常换装，收紧稳定外观与表达节奏，并改善当前穿搭和表情的连续体验。

### 👗 穿搭轮换与长期偏好

- 修复夜间复盘把模型自产穿搭反向学习成长期偏好、继而反复生成同一配色和睡裙的自强化循环；既有自主外观偏好记录无需迁移，生成上下文会自动忽略，后续也不再继续写入。
- 全天日程初始穿搭、计划换装、实时换装、邀约后的穿搭检查和生图真实换装统一采用已启用的视觉衣橱；新主体服装必须选择完整套装或上装与下装组合，衣橱详细描述作为最终服装事实，不再只作模糊灵感。
- 用户本轮明确换装要求和已经发生的当前穿着事实仍保持最高优先级；长期偏好只参与衣橱候选排序，近期使用记录负责轮换，不会把高偏好单品固定成每日制服。

### 💬 回复延迟

- 保留模型语义分段和自然分段兜底；各分段继续按自然随机间隔发送，不会因模型规划耗时压缩后续表达停顿。

### 👤 人设外观与整组造型

- 选择同图整组造型时，仍可一起采用识别出的服装、发型、妆容、美甲和配饰，不削弱视觉衣橱的完整搭配能力。
- 穿搭更新会同时读取角色人设、用户原始请求和整组展开结果；人设明确的自然发色等稳定外观事实优先，只有用户原话明确要求改变或可靠生活事实确认变化时才允许覆盖。
- 全天日程生成、实时换装和生图最终外观快照共用同一优先级，衣橱参考图或同图候选不会再把角色稳定外观写成另一套设定。

### 💄 妆容与美甲展示

- 妆容和美甲与发型统一拆分为简短名称和详细描述；Dashboard 以标签显示名称，以普通正文显示细节，不再把整段识图描述塞进标题标签。
- 全天日程、实时换装、视觉衣橱采用结果、聊天隐藏上下文和生图生活上下文共用新的名称/细节字段，后续生成可以稳定延续同一外观事实。
- 旧记录无需数据库迁移；没有独立名称的历史数据会显示通用的“妆容”或“美甲”标签，原有详细描述仍完整保留。

### 🖥️ Dashboard 展示

- 当前穿搭详情沿用时间轴的内部纵向滚动交互，并采用更紧凑的自适应高度；标题与下方穿搭判断保持可见，两列底部大致对齐，桌面端、移动端和键盘操作均可查看完整服装、发型、妆容与美甲信息。

### 😺 表情轮换与投递

- 普通回复、主动互动和 Agent 表情工具统一按会话轮换；最近 5 次成功发送过的表情不会再次进入候选，候选都刚用过时保留文字，不为了发表情而重复发送同一素材。
- 主动互动发送成功后也会写入表情使用记录；重启后继续沿用同一轮换边界，不会因内存状态重置而立刻复用。
- 同一来源消息已发送过表情时，会在模型选择前直接跳过，避免重复投递和无意义的额外模型调用。
- 数据库结构保持 v13，不新增迁移；已有表情素材和历史表达记录自动参与后续轮换。

## 🌸 v1.2.7 · 2026-08-16

> 💗 收紧时间轴终态、夜间复盘并发恢复和微信后台投递验证，并完成视觉衣橱导入与管理体验的收尾。

### 🕰️ 时间轴执行终态

- 时间轴合法执行状态改为模型层单一契约，结算、时钟推进、夜间复盘和数据库保存统一识别“待进行、进行中、已完成、已过期、已跳过、已取消”。
- 等待真实回执的拍照、视频、社交和聊天动作到期后，`expired` 不再在时间轴去重、保存或重新加载时被降回 `planned`；动作结果、时间轴展示和后续结算保持一致。
- 增加动作到期后的内存状态、数据库状态和重新读取回归测试，防止终态定义再次在不同模块间漂移。

### 🌙 夜间复盘并发恢复

- 夜间复盘结算动作遇到日记录版本冲突时，按短退避重新读取最新时间轴后继续，不再连续紧贴数据库耗尽三次机会。
- 同一轮时间轴已经被另一个任务完成结算时直接复用完成标记；仍有持续冲突时保留未完成阶段交给持久任务重试，不覆盖较新的日程或重复调用复盘模型。
- 保留模型语义分段及自然分段兜底；本次只修复持久化与后台并发，不以短回复强制跳过语义判断改变既有聊天表达。

### 📮 微信投递回归

- 微信好友、微信群、后台文字、主动承诺、私聊回访、语音、图片、套图、视频和重启媒体恢复继续共用真实平台投递入口。
- 补充 QQ 与微信实例 ID 相同时的好友路由、微信群会话类型、微信适配器未就绪禁止错误回退，以及普通平台正常转交 AstrBot 的回归测试。
- 当前消息仍优先使用原始事件发送；脱离事件的后台任务根据 `@im.wechat` 与 `@chatroom` 构造对应微信会话，不要求数字 session ID。

### 👗 视觉衣橱管理

- 导入识图在指定视觉模型不可用时降级到当前默认模型；衣橱素材直接保留适合生图复用的详细视觉描述。
- 待确认素材可以在详情中明确启用；归档统一调整为停用语义，来源网页使用新标签页打开，重新识别、喜欢、不喜欢、启用、停用和删除保持独立操作。
- 管理页按图片来源聚合同一造型拆出的套装、上装、下装、鞋袜、配饰、发型、妆容和美甲；类别与数量横向展示，修复下拉菜单、移动端标签截断和批量操作换行。
- 数据库结构保持 v13，本次不新增迁移；旧衣橱和既有生活数据无需清空、重建或重新分类。

## 🌸 v1.2.6 · 2026-08-15

> 💗 收紧真实交流场景、主动承诺、穿搭外观、视觉衣橱、图片导演和跨平台投递闭环，并完成后台任务恢复。

### 🫂 交流现场与主动承诺

- 将“通过哪个平台收到消息”和“角色与对方是否正在同一地点交流”拆成两个独立事实；面对面场景不再因为消息来自 QQ 或微信就描述成隔着手机聊天，旧场景事实不会跨轮次覆盖当前明确语义。
- 当前轮次的共同在场、远程交流和待核实状态只对实际参与者生效；缺少新证据时回到待核实，不再沿用其他联系人或旧对话的交流方式。
- “到时候我叫你”“晚点提醒你”等明确后续动作会在具备对象、时间和私聊范围时创建持久回访任务；任务重启后仍可继续，面对面即时承诺则自然回应，不会误结算成其他会话的共享承诺。
- 私聊闲时回复和私聊回访会先检查共同在场事实；角色就在对方面前时不再额外发起平台回访。

### 👗 穿搭事实与视觉衣橱

- 妆容成为独立的当前外观事实：已确认妆容可以单独更新并进入状态、上下文和生图提示，不再因为服装未变化而持续显示“未知”。
- 拍照、套图和视频生成会先对齐当前服装、发型、妆容、美甲与配饰，只应用用户本轮明确要求的变化；生成媒体不会再无依据换掉刚确认的造型。
- 视觉衣橱从“服装、发型”扩展为套装、上装、下装、鞋袜、配饰、发型、妆容和美甲八个独立类别；完整造型图可以拆出可靠单品，低置信度候选进入待确认，不直接污染当前穿搭。
- 衣橱增加感知哈希去重、相似候选复核、批量清理、偏好反馈、备份与恢复；自动搭配按类别均衡读取候选，发型、妆容与美甲只在明确采用时变化。
- Dashboard 衣橱管理对齐表情管理的统计、筛选、分页和操作布局，补齐八类筛选、来源预览、状态管理与移动端响应式排版。

### 🛡️ 后台任务与模型链路可靠性

- 到期主动承诺遇到 HTTP 400、403、404、422 等不可重试的模型能力或参数错误时，会直接进入终态，不再按分钟反复重排；主动任务可单独指定可用模型，留空时仍跟随默认模型。
- 日程与生活裁定的单次模型调用增加可配置超时；视频关键帧差异筛选移出事件循环，避免同步图像计算阻塞 AstrBot 其他消息。
- 图片失败回复只展示可操作的业务原因，不再向聊天泄露接口路径、HTTP 状态或下游网关原文；内部日志仍保留完整排障依据。
- 主动承诺、私聊回访和事实连续性审计在 JSON 语法损坏时进行一次有界修复；格式本来正确时不增加模型调用。
- 夜间复盘改为可恢复的分阶段事务：完整结构化复盘结果会随主记录保存，事件、决策、时间轴、长期演化和记忆沉淀全部完成后才写入完成标记；并发修改或后处理失败时复用原结果继续执行，不重复调用模型，也不再用旧时间轴覆盖新状态。
- 持久媒体任务使用独立工作租约和投递回执；重启只恢复一次尚未完成的产物，正在投递的任务不会被第二个工作器重复领取。

### 🎬 图片导演与媒体表达

- 语音、表情、图片、套图、改图和视频统一使用结构化工具回复政策：语音和表情静默工具前旁白，只有语音成功后替代重复文字；图片与套图继续允许自然等待语，视频不额外生成完成后补话。
- 图片导演新增独立模型设置，留空时继续使用当前默认模型；单图身份路线、自由构图和套图镜头规划统一复用该设置，图片安全拒绝后的轻量润色仍保持独立模型配置。
- 自由构图会把用户原始画面要求作为最高优先级硬约束保留，再追加导演生成的构图、可见范围、取景逻辑和连续性方案，避免整理过程中丢失用户明确的主体、动作、穿搭、比例或风格。
- 图片导演超时、返回空对象或结构无效时不再直接取消单图生成，而是保持用户原始提示词继续调用图片接口；明确的当前角色、合影和反推提示词仍沿用原有直出边界。
- 同一工具轮次的媒体产物、等待语和最终文字按事件与回执去重；视频、图片或后台文字即使经过重试和恢复，也不会因为两个发送路径同时完成而重复投递。

### 📮 微信后台投递路由

- 后台文字、主动承诺、私聊回访、闲时回复、语音、图片、套图、视频和重启媒体续投统一通过真实平台类型投递；QQ 与微信适配器使用相同实例 ID 时，不再把微信用户标识误交给 QQ 发送器。
- 当前消息仍优先使用原始事件发送；脱离事件的持久任务会根据 `@im.wechat` 或 `@chatroom` 选择 `weixin_oc` 实例，旧会话和旧数据库记录无需迁移。
- 微信适配器尚未就绪时返回未投递状态并保留任务重试，不会回退到其他同名平台造成错误发送。

### 🖥️ Dashboard 与工程结构

- 数据库结构从 v12 自动升级到 v13，重建视觉衣橱类别约束以支持套装、上装、下装、鞋袜、配饰、发型、妆容和美甲；旧候选、来源、反馈和使用记录在事务中迁移保留。
- 记忆证据、认知事实、行动来源和关系范围统一解析联系人昵称；内部枚举、证据字段、状态值和异常回退补齐中文展示，专业协议名与模型名继续保留原文。
- 运行时上下文快照改用受控隔离 Runner 执行数据库读取，允许内部读取出现真实异步挂起；必要运行时钩子缺失时立即报错，可选平台能力仍可正常缺省。
- 生产包目录和 Python 模块统一为唯一的单个小写英文单词，消除包与模块重名、跨目录同名和下划线模块；新增结构测试防止后续重新引入路径歧义。
- 英文运行日志和普通代码说明统一调整为中文，`HTTP`、`JSON`、`LLM`、`API`、`Dashboard`、`Runner` 等专业词保留；高频耗时和恢复提示降为调试日志，减少正常运行时告警刷屏。
- 删除零引用的日程调整枚举和旧裁定辅助模块；已发布数据库兼容列继续冻结保留，不新增高风险清理迁移。

## 🌸 v1.2.5 · 2026-08-13

> 💗 明确私聊生活动作的权限语义，拆分地点审计、Dashboard 状态快照与运行时上下文边界，在保持现有功能和数据兼容的前提下降低核心流程复杂度。

### 🔐 私聊动作权限语义

- 将内部容易误解为“关系白名单”的动作范围统一命名为“私聊范围”：AstrBot 管理员始终允许，任意私聊会话允许，普通群聊成员拒绝；权限判断不依赖昵称、关系档案或模型推断。
- 私聊范围是会话类型边界，不额外建立“主人”白名单。多个联系人使用同一插件实例时，仍共同影响同一角色的全局生活状态；全量清空、日程重生成、生成时间和平台登录管理继续保持管理员专属。
- 补充管理员、普通私聊、其他私聊、普通群成员和群管理员的确定性权限矩阵测试，避免后续新增工具时把“关系”“私聊”和“管理员”三种语义再次混用。

### 🧭 地点审计职责拆分

- 将全天地点审计拆分为候选解析、地点确认、跨城判断、通用地点降级、路线校正和行动同步等独立阶段，保留原有地图服务商、地点预选和失败回退行为。
- 已预选地点、具体场所、泛化外出地点、居家节点、线上活动和途中节点继续使用原有结构化字段；本次不改变日程 JSON、路线缓存或 Dashboard 时间轴格式。
- 地图失败仍按既有规则保留可用日程并记录校正结果，不新增模型调用，不扩大地点替换范围。

### 🖥️ Dashboard 与上下文快照

- Dashboard 状态聚合拆分为世界事实、生活体验和运行时状态三类快照，顶层响应字段保持不变；各板块继续只展示当前业务日有效的过程记录。
- 运行时上下文仓库改用显式协议声明其数据库读取能力，保留“一次数据库快照、一次工作线程切换”的读取边界，不在数据库线程创建嵌套事件循环。
- 补充快照仓库契约与 Dashboard 静态回归测试，并消除测试对源码换行格式的依赖，重排或格式化代码时不再产生无意义失败。

## 🌸 v1.2.4 · 2026-08-12

> 💗 收紧连续消息的生成期接续，补齐视觉衣橱学习与反馈闭环，并提高联网学习在图片防盗链、失效候选和单轮搜索不足时的成功率。

### 🔐 Agent 动作权限与数据归属

- 新增统一生活动作提案与策略裁定层：聊天 Agent 负责提出查询或变更，确定性引擎在执行前统一检查管理员身份、私聊边界、会话归属和动作影响范围；数据库事务、任务恢复和媒体投递不交给模型自由执行。
- 普通群成员不能清空数据库、重生成日程、修改生成时间或借图片、地图、邀约和衣橱工具改动全局生活；B 站登录、登出和状态管理统一限制为 AstrBot 管理员操作。
- 承诺列表按来源会话隔离，完成、取消和延期会先读取记录并校验归属；只有创建会话或管理员可以修改，缺少来源的旧记录默认仅由管理员维护。
- 跨会话历史、世界记忆、偏好、事件、配置和复盘改为管理员资料；当前状态、今日日程、周计划、后续安排、时间轴和天气仍保留日常查询能力。

### 🛡️ 模型链路与升级闭环

- 大语言模型 HTTP 400、403、404 和 422 等参数或能力错误不再无效重试；429、超时和 5xx 瞬时故障采用指数退避，连续失败时对当前提供商短暂熔断，并在已有默认模型回退条件满足时继续处理。
- 数据库结构升级为 v12，新增视觉衣橱候选及来源、使用与反馈记录；旧数据库在启动事务中自动迁移，保留既有生活、记忆、日程和媒体数据。
- AstrBot 兼容范围收紧为 `>=4.26,<5`，避免未来 5.x 破坏性接口变化被误判为已兼容；README 补充消息侧权限、承诺归属和生产日志隐私说明。

### 🎨 Grok 图片协议后续兼容

- 在已发布的文生图与图生图通道基础上，统一处理 Grok 的 `aspect_ratio`、`resolution` 和 `stream` 参数；不再附加可能覆盖比例的非文档 `size` 字段，图生图使用独立的 `/v1/images/edits` 请求结构。
- 图片结果同时兼容 Base64 和结果地址；返回地址会按图片内容下载和校验，失败只记录当前通道原因，不把错误结果写入生活记录。

### 💬 连续消息与回复收束

- 用户在模型已经开始生成回复时继续补充的普通消息，会加入同一个进行中的话轮；最新有效请求会读取合并后的完整内容，不再被第二次响应门控误判为无需回复。
- 过期事件、旧轮次结果和重复工具文字继续按事件与会话隔离；只有实际成功投递的工具结果才参与本轮去重，避免出现日志有生成但用户收不到，或同一句被重复发送。
- 保留命令、引用、图片、语音、视频、文件和工具结果的独立处理边界，不扩大连续文字收束的匹配范围。

### 🧥 视觉衣橱浏览学习

- 联网学习采用“快速检索 → 深度检索”的两阶段流程：快速结果不足以满足目标数量时自动继续深度检索，跨轮次按图片地址去重，并设置候选尝试上限，避免只搜一轮后直接报告失败。
- 远程参考图下载增加浏览器请求头、来源页 `Referer`、独立连接/读取超时和内容校验，降低商品站防盗链、重定向到 HTML、失效链接和单站点慢请求导致整轮学习失败的概率。
- 只有成功下载的真实图片、通过视觉模型事实核验且符合服装/发型类别的候选才会写入衣橱；网页标题、营销文案和失败候选不会伪装成造型记录，来源页面仍会保留便于追溯。
- 快速检索本身失败时会隔离该轮错误并继续后续可用检索；最终仍没有有效候选时才返回明确失败原因，不会写入半成品数据。

### 👗 穿搭变化与当前事实

- 普通穿搭和近期记录高度重复时继续执行保存前重写；睡衣允许自然复穿，但连续多日仍是高度相似的具体服装、主色、材质和版型组合时，会要求只调整造型而不重做日程。
- 同日强制重生成时，旧日程与旧穿搭只用于识别重复，不再作为必须 `keep` 的当前事实；新的天气、活动和角色审美可以重新决定造型。
- 实时穿搭判断分别审视主体服装、鞋履、外层和随身配饰；不适合持续居家、家务、休息或睡眠活动的局部组成可以单独调整，不需要为了换鞋或脱外套整套换装。
- 已发生日程或实时状态明确确认的新穿搭仍具有事实优先级；未来换装和仅用于防重复的历史造型不会提前覆盖当前穿着。

### 🏃 运动、饮食与食谱闭环

- 日程生成新增独立身体活动判断：结合体力、身体状态、天气、近期运动负荷、日程密度和角色兴趣决定是否安排运动，也允许合理地不安排，不会为了填满生活实况面板强行增加锻炼。
- 只有以锻炼、身体舒展或恢复为主要目的的明确节点才保存为运动；通勤、购物、逛街、游览、社交和拍照中的普通走动继续按真实目的记录。运动节点必须同时提供项目、时长和强度，完成结算后才进入运动负荷。
- 餐食动作明确分为直接用餐、居家烹饪和点餐外卖：`meal` 不再校验或扣减家庭库存，`cook` 才按结构化食材检查库存并扣减，`order_food` 保持独立语义，避免外出或现成餐食因家里没有食材而结算失败。
- 已完成烹饪不再依赖模型填写食谱编号，系统按菜品生成稳定编号并沉淀食谱；插件启动时会幂等补齐历史已提交烹饪遗漏的食谱及餐食关联，Dashboard 饮食页同步展示食谱、食材和标签。

### 💬 工具回复与连续话轮

- 主模型同时返回工具调用前文字和同内容的 `send_message_to_user` 时，只保留一次实际发送；去重限定在同一事件、同一会话和完整纯文本一致的工具轮次，不影响不同内容、跨会话消息或用户有意重复发送。
- 多工具调用不再依赖执行顺序：发送工具先执行时由发送工具持有该段文字，图片或视频工具先执行并已发送前置回复时，后续相同发送工具会直接复用成功结果，不再让语义分段重复运行和投递。
- 用户在模型已经开始生成回复时继续补充的普通消息，会加入同一个进行中的话轮；最新有效请求会读取合并后的完整内容，不再被第二次响应门控误判为无需回复。
- 保留命令、引用、图片、语音、视频、文件和工具结果的独立处理边界；过期事件、旧轮次结果和重复工具文字继续按事件与会话隔离。

## 🌸 v1.2.3 · 2026-08-11

> 💗 补齐 Grok 图片协议、穿搭多日变化与生活实况运动饮食闭环，让生成结果、结构化记录和 Dashboard 展示保持一致。

### 🎨 Grok 图片与文转图排版

- 文生图和图生图接口通道新增 `Grok Image` 类型，默认模型为 `grok-imagine-image`；分别调用 `/v1/images/generations` 与 `/v1/images/edits`，兼容接口返回 Base64 图片或结果地址。

### 👗 穿搭变化与当前事实

- 普通穿搭和近期记录高度重复时继续执行保存前重写；睡衣允许自然复穿，但连续多日仍是高度相似的具体服装、主色、材质和版型组合时，会要求只调整造型而不重做日程。
- 同日强制重生成时，旧日程与旧穿搭只用于识别重复，不再作为必须 `keep` 的当前事实；新的天气、活动和角色审美可以重新决定造型。
- 实时穿搭判断分别审视主体服装、鞋履、外层和随身配饰；不适合持续居家、家务、休息或睡眠活动的局部组成可以单独调整，不需要为了换鞋或脱外套整套换装。
- 已发生日程或实时状态明确确认的新穿搭仍具有事实优先级；未来换装和仅用于防重复的历史造型不会提前覆盖当前穿着。

### 🏃 运动、饮食与食谱闭环

- 日程生成新增独立身体活动判断：结合体力、身体状态、天气、近期运动负荷、日程密度和角色兴趣决定是否安排运动，也允许合理地不安排，不会为了填满生活实况面板强行增加锻炼。
- 只有以锻炼、身体舒展或恢复为主要目的的明确节点才保存为运动；通勤、购物、逛街、游览、社交和拍照中的普通走动继续按真实目的记录。运动节点必须同时提供项目、时长和强度，完成结算后才进入运动负荷。
- 餐食动作明确分为直接用餐、居家烹饪和点餐外卖：`meal` 不再校验或扣减家庭库存，`cook` 才按结构化食材检查库存并扣减，`order_food` 保持独立语义，避免外出或现成餐食因家里没有食材而结算失败。
- 已完成烹饪不再依赖模型填写食谱编号，系统按菜品生成稳定编号并沉淀食谱；插件启动时会幂等补齐历史已提交烹饪遗漏的食谱及餐食关联，Dashboard 饮食页同步展示食谱、食材和标签。

## 🌸 v1.2.2 · 2026-08-09

> 💗 将语音能力迁移到火山引擎，并补齐角色外观、真实换装、语音投递、交通安排、移动端交互和启动时序的最终结果闭环。

### 🎙️ 火山引擎语音合成

- 语音供应商由 SiliconFlow 完整替换为火山引擎语音合成 2.0；删除旧接口地址、模型、音色和情绪音色映射配置，不保留双供应商兼容分支。
- 支持火山预置音色和声音复刻 2.0 音色，分别自动使用 `seed-tts-2.0` 与 `seed-icl-2.0`；复刻音色使用官方默认的 `seed-tts-2.0-standard` 合成模型，不发送不受支持的 `context_texts`。
- 语义表达裁定直接返回 `neutral`、`happy`、`light`、`sad` 或 `angry` 语音风格枚举，再转换为语速、音量和音调参数；不再从自然语言情绪描述中做关键词匹配。预置音色额外使用官方支持的语气指令，复刻音色不发送不兼容的 `context_texts`。
- 插件只调用火山控制台已经创建好的音色，不增加上传样本、训练轮询、试听管理或声音复刻数据库。用户只需填写 API Key、音色类型和音色 ID。
- HTTP/SSE 音频流支持跨分块解析、请求超时、瞬时错误退避重试、音频大小限制、原子文件写入和参数化缓存。
- 语音工具状态按当前事件和消息隔离；只有本轮语音真实投递成功后，才会隐藏最终的重复文字，上一轮残留状态不会让后续回复凭空消失。
- 自动语音发送改为纯语音组件，移除适配器可能再次展示的文字标题；发送失败时保留文字回退，不再出现“日志显示已发送、实际没有收到”的假成功。
- 长文本按 UTF-8 字节限制和自然标点拆分，分段合成后合并为一条语音；声音复刻音色不读取本地命中缓存，控制台使用同一音色 ID 重新训练后，下一次请求直接采用新音色。
- 旧版 SiliconFlow 配置不会自动转换为火山凭证，升级后需要在语音设置中重新填写火山引擎 API Key 和音色 ID。

### 👗 角色外观与真实换装

- 当前角色首次参与图片生成时，会等待从当前人设中提取稳定外貌资料后再开始生图；不会因为后台提取尚未完成而让第一张照片遗漏既有人设体貌。
- 当前角色的真实换装完成落库后，确认照会把已保存的穿搭、鞋饰、风格和发型作为唯一权威造型；原始消息中的“换一套”等动作意图不会在生图阶段再次触发第二次换装。
- 单人照和合影中的当前角色统一遵循最终造型快照，场景、动作、构图、参考图和工具补充提示都不能覆盖已经生效的真实换装。
- 普通试穿、创作图片和视频仍允许用户明确指定临时视觉造型，并继续与真实生活穿搭状态分离。

### 🧭 交通选择与生活上下文

- 自动交通方式不再固定优先步行，而是比较地图返回的距离、耗时、天气、体力和日程可用时间；高温、雨雾、低体力或较长路线会降低步行和骑行优先级。
- 不会只因为驾车更快就假定角色具备车辆条件；用户或既有日程明确指定的交通方式仍保持最高优先级。
- 公共交通路线会读取地图返回的结构化换乘方案，时间轴可区分“公交”“地铁”和“公交 + 地铁”；服务商未返回可识别明细时稳定回退为“公共交通”，不由模型猜测。
- 修正 Dashboard 把 `travel_mode=transit` 先按地点类型翻译成“交通点”的枚举冲突；地点类型中的交通节点与路线方式中的公共交通现在分别解释。
- 已校正的出发地、目的地、交通方式、公共交通细分、预计用时、距离和地图来源会进入普通聊天与穿搭判断上下文，后续回复和造型决策能够读取真实出行安排。

### 🖥️ Dashboard 交互

- 时间轴节点较多时改为在列表内部纵向滚动，面板标题和编辑操作保持可见；桌面端、移动端和键盘操作均可直接浏览完整日程，编辑时新增节点会自动滚动到可见位置。
- 移动端滑块会先识别手势方向：纵向滚动不改变数值，只有明确横向拖动后才提交调整，避免误触后数值短暂变化再恢复。
- 图片接口条目支持通过拖动手柄在触屏设备上排序，拖入目标位置时其他条目实时让位；从第二项移动到第一项、取消拖动和键盘排序均保持稳定。

### 🔌 启动与升级

- 外部图片生成入口支持分别指定文生图与图生图模型，并只在对应模式中选择模型名完全一致的接口通道；旧的单一模型参数继续同时作用于两种模式，同一模型的多条线路仍按原顺序容错。
- 数据库结构升级为 v11，为时间轴和路线缓存增加地图公共交通摘要；旧数据库会在启动事务中自动补列，既有日程和路线记录保留，不额外调用模型或要求重新生成。

## 🌸 v1.2.1 · 2026-08-08

> 💗 收紧多模态画面、生活状态、记忆召回与日程密度之间的事实边界，避免生成内容擅自换装、把计划意愿误显示成当前活动或绕过明确配置。

### 🖼️ 多模态造型连续性

- 当前角色生图、照片套图和视频生成在请求创建时读取当前生活中的穿搭、风格和发型，并以权威造型快照传入后续首帧、镜头规划和媒体生成流程；模型不会再根据地点、动作或自行扩写替换当前造型。
- 用户原始消息明确要求试穿、换造型或采用另一套视觉外观时，仍可覆盖快照；这类视觉尝试不会自动写成真实换装。
- 使用用户图片或上一张成功图片作为视频首帧时，以首帧中的实际造型为连续性依据，不强行覆盖参考图。
- 好友合影的当天造型只在首次建立、实际穿搭/发型变化或场景切换时提交；同一造型沿用和套图重试不会重复刷新好友造型记录，旧版套图清单仍可正常重试。

### 🚪 Dashboard 活动状态

- “活动倾向”展示改为“活动状态”，优先读取时间轴执行状态、地点类型、旅行范围和途中标记；在公园、商场等具体地点时显示“外出中”，交通节点显示“途中”或“旅行途中”。
- 已取消、已跳过、已过期和已完成的节点不会继续冒充当前活动；只有缺少有效地点事实时，才使用体力、外出意愿、社交意愿等状态指标作为兜底。
- 保留“外出意愿”等状态指标的原有含义，不把意愿值改写成当前地点事实。
- 时间轴地点、地点类型、城市、地址、坐标、路线来源和交通耗时现在会随日程持久化，刷新或重启后仍与当前时间轴一致。

### 🧠 记忆召回与日程密度

- 模型语义记忆排序会先读取每类注入上限；上限为 `0` 时严格返回空结果，不再因为先加入候选、后检查数量而意外注入一条记忆。
- 日程生成不再在可配置的时间轴规则之后额外追加“8-12 个节点”或“约 3 小时”的固定提示；节点数量根据生活跨度、活动复杂度和真实状态变化自然增加，不设固定上限。
- 配置面板的时间轴默认规则与运行时统一默认值保持一致；自定义规则不会再被重复的内置数字覆盖，系统仍会校验完整生活覆盖和明显过大的时间空白。

## 🌸 v1.2.0 · 2026-08-08

> 💗 让聊天中已经发生或确认的生活变化真正成为当前事实，同时收紧分段、动图理解和 Dashboard 的展示边界。

### 🗓️ 聊天约定与当天日程

- 普通聊天、批次记忆和会话行动项中提炼出的当天同行、见面、出行与准备事项不再只停留在行动项；确认属于当前角色且仍可执行时，会继续合并进当天未来时间轴。
- 合并过程保留已经发生和正在发生的节点，只重排尚未开始的安排；地点、路线、交通方式和时间顺延继续经过当前地图服务复核，避免聊天约定绕过日程地点审计。
- 地图、模型或临时网络问题导致的协调失败会保留为可重试状态，不会提前标记为已处理；后台巡检可以继续补偿，同一承诺成功或明确跳过后不会重复改写时间轴。
- 聊天约定附带的穿搭要求仍按实际生效时间处理，不会因为傍晚计划提前覆盖下午的当前穿搭。

### 👗 当前穿搭事实

- 穿搭判断新增当前事实来源：数据库已有穿搭、已经发生的换装日程和实时生活状态。日程或状态已经明确“换上睡裙”等换装事实时，会更新当前穿搭，不再错误地保留白天造型。
- `keep` 只表示数据库中的现有穿搭仍然有效；模型返回了发生后的新造型时，系统会按居家、睡眠或外出场景校正为真实换装决定，并继续独立保存穿搭、风格、发型名称和发型细节。
- 穿搭判断、消息与动作裁定、生活决策、生活片段和主动决策轨迹的展示说明不再重复时间轴中的 `HH:MM` 钟点；准确时间仍保留在时间轴、承诺、状态日志和证据字段中，`9:16` 等图片比例不会被误清理。
- 旧数据库记录无需批量改写；读取历史说明和打开 Dashboard 时会应用同一展示规则。

### 💬 分段与标点

- 模型原文中的显式换行现在是稳定的表达边界。语义分段可以继续细分每一行，但不能再把上一行结尾与下一行开头错误合并。
- 后台回复、工具回复、模型语义分段和自然分段兜底统一遵守原始换行；类似“先确认感受”与“再答应拍照”的两层语义会按原顺序发送。

### 🖼️ GIF 动图理解

- 检测到 GIF 时，先复用配置的视觉模型完成图片上下文识别，再把识别摘要作为临时视觉事实交给主聊天模型；不支持 GIF 的主模型不再直接接收原始动图而报错。
- GIF 原始输入会从主模型请求中移除，普通静态图片仍按原有多模态链路传递；视觉识别未完成时会明确要求主模型不要猜测画面。
- 后台表情素材采集、图片上下文识别和主模型桥接共享同一份准备结果，避免为同一张动图重复下载或重复识别。

### 🖥️ Dashboard 与移动端

- 会话摘要、群聊环境、消息留意、动作裁定、生活片段、决策轨迹、状态变化和生活实况流水等过程记录默认只展示当前业务日，避免昨天的过程数据继续混入今天。
- 关系档案、偏好、地点、长期事实、库存、食谱、家务轮换等跨日资料继续保留；尚未完成的会话行动项即使创建于前一天，也会继续显示到完成或失效。
- 移动端设置页会先判断滑动方向：纵向手势用于滚动页面并恢复滑块原值，明确横向拖动时才调整数值，减少浏览设置时误改配置。

## 🌸 v1.1.9 · 2026-08-07

> 💗 把短时间连续发送的消息收束成一个完整话轮，并补齐生活动作从结算、专属记录到 Dashboard 展示的闭环。

### 💬 连续消息统一回复

- 私聊中同一用户短时间连续发送的普通文字会先合并为一个话轮，再由最新有效事件统一请求模型；较早事件会停止处理，已经过期的生成结果和后续分段不会继续发送。
- 默认基础等待为 1.5 秒，最长收束等待为 4 秒；语义裁定认为用户仍会补充时可以等待到本轮上限，到达期限后必须继续回复，不会无限挂起。
- 命令、引用、图片、语音、视频、文件和工具结果不进入文字收束；群聊默认关闭，可在“聊天表达”中单独开启，避免多人消息被错误合并。
- “启用聊天表达”关闭后恢复每条消息独立处理；也可只关闭语义等待，保留固定时间窗口的轻量收束。

### 🍚 生活实况记录闭环

- 家务、饮食、采购、运动等结构化动作统一兼容自然语言强度和简写列表，避免模型返回“轻度”等文本值或简写食材时因类型转换失败而丢失专属记录。
- 已提交的生活动作会再次执行幂等领域落账；即使主动作结果已经存在，也会补齐缺失的家务、库存、用餐或运动记录，不再因历史结算成功而永久跳过修复。
- 插件启动时自动扫描并修复旧版本中已提交但缺少专属记录的动作，同时为已有承诺补齐会话行动项；批次聊天记忆新提炼出的承诺也会同步进入行动项。
- 修复过程只根据已有结构化证据补齐数据，不凭空生成历史运动、饮食或家务记录；重复启动不会重复写入。

### 🏷️ Dashboard 来源名称

- 生活实况的会话行动项优先显示关系备注或已保存称呼，其次读取平台昵称；群聊显示群名称，不再把内部统一消息来源标识直接展示给用户。
- 平台暂时无法解析名称时显示“私聊会话”“群聊会话”或“会话”，原始标识仍只在内部保留，不需要迁移既有行动项。

## 🌸 v1.1.8 · 2026-08-06

> 💗 让聊天里已经确认的同行、改约和穿搭要求真正进入当天生活，并完善 QQ 文字转发的身份显示。

### 🗓️ 当天约定执行闭环

- 邀约裁定提出其他时间后，会把改约方案保存为待确认状态；对方随后表示同意时，模型能接续上一轮方案并重排当天尚未发生的节点，不再只留下聊天回复或孤立承诺。
- 单条聊天承诺、批次记忆提炼、`life_commitment` 工具和手动新增承诺统一接入当天协调流程；即使全天日程已经生成，也会判断承诺是否属于当前角色、是否由双方确认、是否仍能在今天执行。
- 协调过程只保留已经发生和正在发生的节点，仅替换未来时间轴；同行人物、地点、交通、准备和返回过程继续经过人物事实与地图审计。说话人自己的单方计划、未确认提议、随口设想和纯偏好不会强行写入当前角色日程。
- 后台生活巡检会补偿重启前后尚未合并的当天有效承诺；同一承诺和同一版时间轴保存稳定协调标记，避免反复调用模型、重复写入事件或在面板刷出相同记录。

### 👗 穿搭时机衔接

- 承诺中明确确认的穿搭要求会保存为结构化待生效指令，并在计划换装时间前约 90 分钟进入穿搭判断；不会在下午过早把傍晚外出造型显示成当前穿搭。
- 新日程包含外出但没有明确指定造型时，仍会沿用原有自主穿搭检查；穿搭协调成功后清理待处理指令，后续普通巡检不会重复换装。

### 📝 QQ 文字转发

- QQ 合并转发文字优先通过 OneBot 登录信息读取当前机器人昵称，同时兼容直接和嵌套返回结构；平台暂时无法提供昵称时显示“机器人”，不再显示笼统的 `Bot`。
- README 的本地验证说明不再把 `pytest.ini` 或 `uv run pytest` 视为插件运行必需条件，继续保留标准库单元测试、编译和配置格式检查命令。

## 🌸 v1.1.7 · 2026-08-06

> 💗 完善 Grok 视频接口的协议兼容、任务识别和首帧画质边界，同时修正 Gemini 文生图的角色参考时机。

### 🎬 Grok 视频接口

- Grok 视频生成兼容中转站 `/v1/videos` 与 xAI `/v1/videos/generations` 两种异步创建路径；任务查询统一回到 `/v1/videos/{任务标识}`，创建结果可识别 `request_id`、`task_id`、`id` 或直接视频地址。
- 新式接口分别提交比例、清晰度和时长，首帧按官方或中转站所需结构包装；接口明确拒绝或未返回任务标识时，自动尝试旧式 `size/n` 参数，并在当前插件实例复用已验证的成功格式。
- 视频比例会归一化到接口支持的最近画幅；默认模型更新为 `grok-imagine-video-1.5`，用户已明确填写的自定义模型不会被覆盖。

### 🖼️ 首帧画质与角色参考

- 视频首帧不超过 20 MB 时保留原图尺寸、格式、画质和完整字节直接提交；只有超限原图才在工作线程中按目标画幅和 720p/1080p 尺寸生成 20 MB 内的临时 JPEG，不会覆盖原始生图。
- 首帧在请求前完成图片有效性校验；无效图片不会继续请求视频接口，日志会分别标明“无需压缩”和“已压缩”。
- 修正 Gemini 文生图在“智能判断”模式下仍无条件附带角色参考图的问题；普通主体和无人画面保持纯文生图，只有图片导演确认当前角色本人入镜时才切换到图生图并传入参考图；“始终参考”和“不使用”的既有语义保持不变。

### ⬆️ 升级边界

- 新增 Pillow 作为首帧图片校验与超限压缩依赖，通过 AstrBot 插件管理重新安装或更新时会按 `requirements.txt` 自动安装。
- 数据库结构保持 v8，不新增迁移，无需清空、重建或重新分类旧数据。

### 🔗 跨插件生命周期

- 插件停止时继续先拒绝新的外部公开入口调用，并等待已经取得租约的调用自然结束。
- 外部调用超过 10 秒排空期限时，会显式取消仍占用旧运行时的任务并等待取消完成，再关闭运行时资源，避免插件卸载后留下孤立任务。
- 同一异步任务重入外部租约时按层级计数，内层退出不会让仍持有外层租约的任务脱离终止追踪。
- 补充外部调用正常排空、超时取消与重入租约测试，明确热重载和停用期间的生命周期语义。

## 🌸 v1.1.6 · 2026-08-05

> 💗 收敛数据库与动作结算的运行边界，并把配置、取消清理和程序化接口固化为可自动验证的工程契约。

### 🗃️ 数据库与运行安全

- 完整上下文快照继续保持一次数据库锁定和一致性读取，同时从约 32 次工作线程往返收敛为一次；不会在数据库线程中创建嵌套事件循环，也不会阻塞 AstrBot 主事件循环。
- 数据库工作线程收到任务取消时会先完成当前查询再释放连接锁，随后原样抛出取消；避免下一条 SQL 与无法被强制停止的旧线程并发使用同一连接。
- 初始化、上传、配置热切换、夜间复盘和操作锁等待在任务取消时仍先完成必要回滚，再原样抛出取消；所有 `BaseException` 清理分支均由契约测试确认最终重新抛出，不会吞没取消或系统级异常。

### 🧭 动作与接口边界

- 生活动作结算按契约校验、前置条件、状态影响和结果提交拆分职责，主结算流程不再同时承担全部判断和写入细节。
- 动作集合明确为 `17` 种：`13` 种内部生活动作允许时间轴模拟结算，社交、聊天、拍照和视频 `4` 种动作要求真实回执；两类集合互斥且完整覆盖，并由统一模型常量和自动测试约束。
- Dashboard 直连路由与自动流程、命令或外部管理使用的程序化接口在注册层明确分开；周计划及 5 个经验管理接口继续保留，完成注册、前台隔离、真实写入和读取验证，但不重新增加已移除的前台操作入口。

### 🧪 配置验证

- 配置回归改为追踪完整的“分组.字段”路径，图片通道等模板列表按“分组.*.字段”校验；不同分组中的同名配置不会再被叶子键名误判为已接入。

## 🌸 v1.1.5 · 2026-08-05

> 💗 让日程地点先经过地图确认再进入最终时间轴，并修复媒体地址安全边界和搜索空输入异常。

### 🛡️ 媒体网络访问

- 恢复图片参考图、聊天表情缓存和远程视频抽取的 DNS 解析检查；默认拒绝回环、内网、链路本地、保留地址、组播地址和云元数据地址，避免任意媒体 URL 直接进入 AstrBot 主机网络。
- 公网媒体和常见代理 Fake-IP 自动兼容，无需新增网络配置或白名单；真实内网、回环、链路本地及元数据地址仍会拒绝。
- 网站地图、整站抓取、网页正文提取和研究报告继续由 Tavily 处理，不套用媒体下载限制，原有自定义网络环境保持可用。

### 🗺️ 日程地点预选

- 日程生成改为“地点意图 → 地图候选 → 最终日程 → 保存前复核”：模型先提取确实需要具体 POI 的外出需求，地图服务确认真实地点、地址和参考通勤时间后，再交给最终日程生成。
- 完整全天按实际需要处理最多 8 个去重地点，局部时段最多 4 个，同时跳过“在家”“附近散步”和线上活动等泛化场景，避免无意义地图请求。
- 完整全天的节点说明改为“至少 8 个、按跨度和复杂度增加、不设固定上限”；系统仍只拦截明显稀疏或存在大段空白的时间轴，不会截断超过 12 个的真实生活节点。
- “大语言模型”设置增加独立的“地点预选模型”；留空时使用当前默认模型，指定后只承担轻量地点意图规划，最终日程仍由原日程生成模型负责。
- 保存前的地点审计会复用本轮已确认的候选和坐标，减少模型自行编造地点后被替换的情况；仍会复核同名地点、城市边界、路线、交通方式和时间轴顺延。
- 地图服务、居住地、Key、网络或预选模型不可用时自动回到原始日程生成和最终校正流程，不阻断首次生成、手动重生成或跨城旅行安排。

### 🧰 可靠性与验证

- 修复网页提取传入空地址时引用未定义变量导致的 `NameError`，统一返回结构化错误结果。
- 后台日程生成任务异常改为输出任务标识和异常详情，避免失败静默。
- 换装纳入虚拟生活内部结算：时间轴完成换装节点后自动生成模拟回执并同步当前穿搭，不再因没有外部回执显示为“已过期”；聊天、社交、拍照、发图和视频继续保留真实回执边界。
- 数据库结构保持 v8，不新增迁移；v1.1.5 验收通过 `1287` 项测试，`8` 项按环境跳过。

## 🌸 v1.1.4 · 2026-08-05

> 💗 修正视频专业总结的时间定位语义，并恢复媒体与网页工具在代理、内网和自定义网络环境中的可用性。

### 🎬 视频专业总结

- “总结与参考建议”作为针对全片的收束内容，不再继承章节时间或显示 `00:00`；有实际素材位置的背景、事实、分析等章节继续保留时间定位。
- 提示规范、结构合并和最终渲染统一执行同一边界；即使模型为收束板块返回了时间字段，成品也不会显示无意义的时间轴。
- 专业总结提示规范变化后，旧格式缓存不会继续复用；重新总结时会按新结构生成，无需手动清理视听缓存。

### 🌐 媒体与网页地址兼容

- 撤回远程视频、表情素材、角色与好友参考图的公网地址及 DNS 解析结果限制，恢复代理 Fake-IP、内网资源和自定义网络环境兼容性。
- 网页读取、批量提取、网站地图和整站抓取不再预先拒绝非公网地址；插件只校验地址非空，并把实际可用性及失败原因交由对应服务返回。

### ⬆️ 升级与验证

- 数据库结构保持 v8，不新增迁移，不需要清空、重建或重新分类旧数据。
- 新增总结收束板块无时间定位、非公网地址透传和视听素材共享准备回归测试；全量验证通过 `1273` 项测试，`8` 项按环境跳过。

## 🌸 v1.1.3 · 2026-08-05

> 💗 修正活动终态、数据库关闭和跨时区缓存边界，让长期运行、插件热重载与旧数据升级保持稳定一致。

### 🕰️ 活动终态与生活实况

- 活动会话不再把已跳过、已过期和执行失败统一保存成同一种状态；时间轴、生活实况和 Dashboard 分别显示“已跳过”“已过期”“执行失败”“已取消”和“已完成”。
- 夜间复盘只收束尚未结束的时间轴节点，不再覆盖已经确认的完成、跳过、取消或过期结果。
- 历史日期中没有真实回执的已跳过、已过期和失败模拟计划不再继续出现在当前生活实况；带有确认、失败或取消真实回执的记录继续保留。

### 🗃️ 数据库与运行可靠性

- 数据库升级为 v8。启动迁移会结合动作回执、动作结果和原时间轴状态，恢复旧版曾被合并为 `failed` 的已完成、已跳过、已过期和已取消活动会话；原有日程、生活实况和真实回执无需清空或手动整理。
- 生活归档关闭后进入明确的终止状态，晚到的后台任务会收到可识别异常，不再重新打开数据库连接；等待数据库锁的任务会在真正执行前再次检查关闭状态，避免插件停用或热重载期间出现空连接错误和连接泄漏。
- 上下文快照移除工作线程中的嵌套事件循环，仍在一次数据库锁定内完成一致性读取；不会为每次快照临时创建和销毁事件循环。
- 生成媒体、反推缓存、视听缓存和表情素材宽限期统一使用真实 Unix 时间戳；修复 UTC 容器或非上海时区宿主机上缓存提前清理、宽限期失效或快照缓存无法过期的问题。

### 🗺️ 地点与工程维护

- 地点审计按字段归一化、地图地点确认、连续路线核验和交通动作同步拆分职责；地点替代、跨城安排、交通方式选择和时间轴顺延行为保持不变。
- 高德地图、腾讯地图和百度地图复用统一的非负数值解析，减少距离、时长、评分和费用字段在不同适配层产生不一致。
- 新增归档关闭竞态、关闭后拒绝重开、上下文快照锁定和旧活动终态迁移回归测试；全量验证通过 `1273` 项测试、`8` 项跳过及 `70` 项子测试。

## 🌸 v1.1.2 · 2026-08-04

> 💗 收紧日程重生成后的地点、出行、造型和生活实况一致性，让当前计划、实际回执与前台展示保持同一版本。

### 🗺️ 日程、地点与出行

- 完整全天日程通常生成 8 至 12 个有意义节点；新增时间轴密度校验，全天只有 7 个节点、跨度过疏或相邻节点长时间空白时会要求重写，持续活动仍不会按整点机械拆分。
- 日程和邀约中的具体地点会在提交前经过当前地图服务校正；同名地点可结合提示消歧，无法确认原地点时优先替换为排序后的真实候选，没有可用候选时降级为泛化场景和默认出行估算，避免整份日程反复失败。
- 地点校正会写回交通方式、预计分钟、距离、出发地和地图服务来源；预留时间不足时可切换为更合适的交通方式，仍不足时顺延尚未开始的节点。明确标记的旅行、出差和跨城安排继续允许使用目标城市，不会被居住城市误判为异常。
- 修复高德公交路线把城市中文名直接提交给城市编码参数导致 `INVALID_PARAMS（20000）` 的问题；现在会先解析并缓存行政区划编码，已经是有效编码时直接复用。

### 👗 造型与生活实况

- 同日强制重生成把旧日程和旧造型仅作为重复检测依据，不再把待替换穿搭当成必须延续的当前事实；模型需要根据新日程、天气和审美重新决定造型。
- 当前穿搭、穿搭风格、发型名称和发型细节继续独立保存；穿搭正文会移除已经由发型字段承载的片段，Dashboard 仍单独完整展示发型，不再在同一卡片重复两遍。
- 同日日程替换后自动清理已经失效且未完成的活动会话；生活实况、模型上下文和统一时间线只保留当前日程版本对应的模拟饮食、家务和运动记录，已经确认、失败或取消的真实回执继续保留。
- 同一动作同时拥有通用活动记录和饮食、家务或运动专属记录时，统一时间线只展示信息更完整的专属记录，避免同一餐食或活动重复出现。

### 🖥️ 前台与升级

- 时间轴补充显示交通方式、预计用时、距离、出发地和路线来源。
- “记忆抽屉 → 世界 → 裁定”恢复 `v1.0.9` 的通用记录方式，不再按类别、来源、阶段与结果细分；主动互动重新使用阶段动作名保存，旧数据库已有分类列继续兼容但不再读取、回填或写入。
- 数据库结构保持 v7，本次不新增迁移、不重新分类历史数据；旧的模拟记录仍保留在数据库中，只在读取当前有效生活实况时按日程版本过滤。
- 新增日程密度、地图候选替换、公交城市编码、跨城旅行、交通回写、强制重生成造型、发型分离、生活实况去重和中文展示回归测试。

## 🌸 v1.1.1 · 2026-08-04

> 💗 收敛每日生活聚合的并发写入、日程替换和消息热路径，避免后台旧任务覆盖最新状态，也减少正常聊天等待。

### 🗃️ 数据一致性与迁移

- 数据库升级为 v7，为每日生活记录增加单调递增版本号；v6 及更早数据库继续在启动事务中自动迁移，原有日程、天气、穿搭、状态、地点和事件不丢失。
- 日记录保存改为乐观并发合并：天气、状态、穿搭、日程等不同字段可以安全合并，同一字段被较新任务修改后会拒绝过期结果，不再由旧对象整日覆盖。
- 天气更新和状态日志追加改为单事务字段修改；模型状态或穿搭结果过期时丢弃旧结果，不再把并发冲突当作普通生成失败。
- 强制重生成不再预先删除旧日程；只有新日程成功解析并完成保存后才原子替换，模型失败时继续保留原有生活安排。
- 修改居住地后建立新的生活上下文边界，清理只对旧居住地有效的地点索引、路线缓存和当前周计划，再在后台重建天气、周计划与当天日程；历史每日记录继续保留，不需要清空数据库。
- 新生活记录生成完成前，当天旧地点、天气、穿搭、心情、状态和时间轴不会继续注入聊天；边界之前的地点、事件和本周进度也不会参与当前城市的日程生成。
- 近期生活惯性改为只提供生活模式与活动倾向聚合，不再重复注入旧穿搭、颜色、发型和逐日素材；新穿搭若与近三天非睡衣造型高度重复，会在保存前被拒绝并只重写造型字段。

### 💬 消息热路径与后台任务

- 保留模型语义分段及自然分段兜底；分段统计区分规划失败、自然兜底和最终单条发送，不再重复累计失败指标。
- 世界记忆与生活体验改为单次联合语义排序，同一轮聊天不再对两组上下文重复发起排序调用。
- 模型语义分段和自然分段仍按配置模拟停顿，但单条回复累计等待最多 8 秒；新消息到来时继续取消未发送尾段。
- 显式标记为聊天类别的记忆采集和普通回复效果学习统一使用聊天并发门，不再因为任务名称不同绕过 `chat_limit`。

### 🧰 可靠性与维护

- 首次生活补全失败会记录异常类型和 10 分钟重试边界，不再静默进入冷却。
- 精简 AstrBot 运行时缺少完整网页工具包时安全降级，避免可选模块导入阶段阻止插件加载。
- 日记录版本合并策略从 SQL 归档中独立出来，并新增数据库迁移、并发保存、失败重生成、后台限流和投递等待预算回归测试。

## 🌸 v1.1.0 · 2026-08-04

> 💗 把日程中的内部生活动作落成可查询、可结算、可迁移的生活实况，同时保留外部行为的真实回执边界。

### 🧭 生活动作与统一时间线

- 新增活动会话，显式计划动作会记录为待进行、进行中、已完成、失败、取消或过期，并与原有动作回执、状态结算和决策轨迹保持同一条主链。
- 休息、用餐、做饭、采购、家务、运动和出行等纯虚拟内部动作可在时间轴完成后生成“模拟回执”；聊天、社交、换装、拍照、发图和视频仍必须等待用户、工具或媒体产物等真实回执。
- Dashboard 新增“生活实况”，统一展示活动、饮食库存、家务轮换、运动负荷和会话行动项；设置页中的“生活实况”紧随“生活背景”展示，并按基础与结算、地点与出行、生活记录、行动项与上下文分组，真实与模拟来源继续分开保存。

### 🍚 饮食、家务、运动与出行

- 新增食谱、食材库存、库存流水和用餐记录；采购增加库存，做饭按结构化食材清单扣减，库存不足时拒绝动作并留下局部重排依据。
- 新增家务周期、上次完成和下次应做时间；运动保存时长、强度和负荷分数，后续日程与聊天按固定预算读取近期记录。
- 地点和出行支持高德地图、腾讯地图与百度地图，可在设置中选择服务商并配置对应的服务端 Key；居住地成为唯一居住位置来源，解析出的城市同时用于天气、地点搜索和公交路线，“家”的坐标自动缓存。
- 三家地图统一提供地址解析、地点搜索、输入提示、地点详情和步行、骑行、驾车、公交路线；百度 BD-09 坐标在适配层转换为插件统一坐标，切换服务商不会混用接口或悄悄回退到另一家地图。
- 新增地点搜索、路线比较、地点详情和限时出行规划四个自然语言工具；当前服务商未配置 Key 时自动隐藏这些工具，返回模型的结果不包含地点坐标。网络、Key 或地址解析不可用时明确保持不可用，路线计算仍可在已有坐标条件下使用本地估算。

### 💬 行动项、表达与兼容性

- 已确认的未来承诺同步为带负责人、截止时间和状态的会话行动项；完成、取消、过期、改期和纳入日程会同步更新，不新增关键词或正则识别链。
- 日程和普通聊天按稳定优先级与字符预算注入生活实况，不根据特定词语临时触发。
- 模型语义分段超时或返回无效时，自然分段会按强标点分组评估局部长句，并为后续句号、问号等强边界预留段数；“语义分段数量”继续作为上限，不会要求短回复强行凑满。
- “记忆抽屉 → 世界 → 裁定”按会话响应、记忆处理、主动互动、表达方式和流程状态细分；每条记录显示稳定的类别、来源、阶段与结果，主动回复和私聊回访不再使用动态拼接动作名落库。

### ⬆️ 升级与数据兼容

- 数据库升级为 v6：v5 领域表和地点坐标结构继续保留，并为动作裁定增量增加类别、来源、阶段和结果字段。旧记录仅按已有动作枚举与主动裁定协议结构回填，不读取或改写理由正文，无需清空或手动重新分类。
- 高德地图仍是默认服务商；腾讯地图和百度地图为可选通道，只有主动选择后才需要配置对应的服务端 Key。

## 🌸 v1.0.9 · 2026-08-03

> 💗 收敛媒体准备、外部地址和后台任务边界，减少重复下载与并发写入造成的失败。

### 🎬 视频与音频

- 聊天视频理解和 B 站专业总结共享同一份物理素材准备结果；不同总结用途只在准备完成后分别生成，不再因为用途不同重复下载。
- 已准备的视频在必剪转写模式下直接本地提取音频，不再为同一视频再次调用 `yt-dlp`；音频和视频缓存均使用临时文件完成后原子替换，避免并发读取半成品。
- 共享准备键不绑定会话范围，同一视频被不同入口或会话同时引用时也能复用；AstrBot 关闭时会同时取消总结任务和素材准备任务。

### 🔒 外部输入与任务可靠性

- 视频直链、表情素材和角色/好友参考图统一拒绝本地协议、回环地址、内网地址、链路本地地址和明显的云元数据地址；域名请求前增加 DNS 解析结果检查。
- 后台任务面板状态增加各类别的丢弃、完成和失败计数；聊天记忆进入队列失败会明确写入告警，不再静默丢失。
- 数据库仍为 v4，不新增无必要的分类表，也不要求清空或重建旧数据。

### 💬 表达与维护

- 极短的单句回应不再为无法拆分的内容额外等待一次模型调用，较长或多动作回复仍走模型语义分段和自然分段兜底。
- 新增媒体共享准备、跨会话复用和外部地址安全回归测试。

## 🌸 v1.0.8 · 2026-08-02

> 💗 修复首次启动日程生成早于消息平台连接的竞态，确保首轮生活背景使用可查询的联系人和历史记录。

### 🚦 启动与平台就绪

- 首次生活初始化在生成当天日程前等待已配置的平台适配器真正就绪；不再把“平台实例已创建”误认为已经连接。
- OneBot/aiocqhttp 会等待反向 WebSocket 客户端建立后再执行联系人昵称、群名和历史记录查询，避免启动日志中出现“适配器尚未连接但日程已生成”。
- 无平台、仅网页聊天或测试替身会直接放行；真实平台连接异常时使用 120 秒超时保护并记录降级日志，不会无限阻塞插件启动。
- 保留现有数据库结构 v4，无需手动迁移数据库。

## 🌸 v1.0.7 · 2026-08-02

> 💗 把生活决策、节律趋势、研究任务和聊天分段的边界补齐，让后台状态与实际投递结果保持一致。

### 🧭 生活决策与趋势

- 修复穿搭更新在保存决策前引用不存在局部变量的问题；保持原穿搭也会留下可追溯的决策证据，不再被上层误报为更新失败。
- 修复同一业务日同时存在短期和持续生理节律时的聚合错误，趋势会分别保留各生命周期的最新有效证据。

### 🌐 研究任务与工具边界

- 研究报告任务成功时收束为 `completed`，失败、取消或超时收束为 `failed`；AstrBot 重启后不会把错误报告误恢复成成功任务。
- 网站地图、整站抓取、研究报告仍使用 Tavily，并继续执行公网地址校验、会话隔离、结果限长和持久任务恢复。
- QQ 专属文本转发继续只面向 OneBot/QQ；非 QQ 平台不会误走合并转发路径。

### 💬 表达与兼容性

- 保留模型语义分段；模型返回结构合法但把多个完整句合并成一段时，才使用通用自然停顿兜底。
- 修复分段消息已经投递但后处理失败时返回“未发送”的问题，避免上层重复发送。
- 数据库结构保持 v4；旧数据库仍由启动迁移流程按事务升级，无需手工分类、清空或重建历史数据。

## 🌸 v1.0.6 · 2026-08-02

> 💗 让状态、动作、记忆和数据清理只保存真正能影响后续生活的内容。

### 🧭 状态与动作闭环

- 后台状态刷新只更新当前状态和日程可行性，不再把模型临时推测自动沉淀为长期偏好或生活事件。
- 计划动作到点后不会自动生成“已执行”回执；没有用户、工具或媒体结果等可验证证据时标记为“已过期”，只有明确回执才会结算状态和时间事实。
- 穿搭保持不变时不再重复写入生活决策，减少穿搭记录挤占日程、关系和承诺记录。
- 当前地点和穿搭继续作为时间事实保存，状态数值不再重复复制到时间事实表。

### 🧠 记忆与节律

- 记忆召回默认使用结构化查询和向量排序；模型语义排序改为显式高级开关。
- 普通聊天效果学习增加独立开关，默认关闭；主动回复的成功投递和后续反馈仍可正常记录。
- 生理节律趋势改为每个日期取一条代表记录，避免最新一天的高频刷新冒充多日趋势。
- 认知运行新增保留天数和实际清理逻辑，当前有效事实不会被清理，已结束任务、轨迹、回执和历史情绪按生命周期回收。
- 全局时间事实在私聊或群聊上下文中可被正确读取，减少“已经记录但聊天看不到”的情况。

### 🌐 外部工具闭环

- 网站地图、整站抓取和研究报告工具继续使用 Tavily，并统一限制为公网 HTTP(S) 地址、深度/广度/页面数量上限和重复页面去重。
- 研究报告任务写入现有可恢复任务队列；AstrBot 重启后会恢复未完成任务的后台轮询，完成或失败结果会收束到原任务记录。
- 研究任务绑定创建它的会话，其他会话不能通过任务编号读取结果；模型语义分段功能和自然分段兜底保持不变。
- QQ 专属文本转发仍只在 OneBot/QQ 可用，原文按会话隔离、限时缓存并通过合并转发发送，其他平台明确返回不支持。

### 🖥️ 工具与前台

- 未启用的图片、视频和语音工具不再进入当前模型工具列表；视频理解、B站自动总结和自动表情收集默认关闭，明确使用时仍可调用。
- Dashboard 默认展示今天、状态、关系、承诺和生活事件；决策轨迹、任务租约、伪聚类和内部节律记录收进诊断层。
- 本地 ASR 安装完成后清理下载副本，避免同一模型重复占用磁盘。

## 🌸 v1.0.5 · 2026-08-01

> 💗 让生活动作从“到点推演”走向“回执确认、事实结算、失败重排与重启恢复”的完整闭环。

### 🕰️ 动作回执与生活事实

- 新增生活动作执行回执，区分已确认、时间轴模拟、失败和取消；图片、套图、视频等真实工具结果会回写对应计划动作，时间轴到点完成则明确保存为模拟证据，不再混同为外部执行成功。
- 动作结算与实时状态刷新会把当前地点、穿搭和数值状态同步为带有效期、来源和替代链的时间事实；聊天或社交动作有明确对象时，也会保存最近互动事实。
- 失败或取消回执会收束对应时间轴项目，保存失败原因和待重排依据；下一轮状态巡检只能替换尚未开始的未来锚点，不能改写已开始、完成或无关的日程。
- 运行时反思正式接入十二小时冷却；达到评分和证据门槛但仍在冷却期时不会重复生成反思。

### 🎨 媒体投递与兼容性

- 单图、参考图编辑、套图和视频在取得产物后、发送前登记持久投递任务；正常送达后立即收束，插件若在两者之间重启，会恢复投递并补交动作回执。
- 恢复任务只发送已经生成的本地文件或结果地址，不会重新调用第三方图片、视频生成接口，避免重启造成重复生成、重复计费或画面漂移。
- 修复视频抽帧、音频提取和 yt-dlp 的 FFmpeg 备用路径缓存；系统 FFmpeg 不可用或运行环境变化时，会重新探测 `imageio-ffmpeg` 提供的可执行文件。

## 🌸 v1.0.4 · 2026-08-01

> 💗 将生活生成扩展为可验证、可恢复、可回放的认知与行动闭环。

### 🧠 认知、证据与长期成长

- 新增时间化事实存储，使用 `ADD / UPDATE / INVALIDATE / NONE` 显式维护有效期、替代版本、来源和证据信号；历史事实保留，但过期版本不会继续作为当前事实注入聊天。
- 新增有门槛的反思机制；只有重要度、证据数量和结构化断言完整时，反思才会晋升为可复用人格认识。
- 聊天记忆批次可直接产出时间事实操作，并锁定真实会话、消息编号和时间，不允许模型伪造其他作用域的来源。
- 隐藏上下文增加当前时间事实、已晋升人格、三层情绪和有证据日记，同时明确情绪不能覆盖人物、消息、图片、日程或工具事实。

### 🕰️ 行动、计划与恢复

- 日程增加 4 至 6 个稳定锚点和未来 3 小时近程细化；局部重排只能调整尚未开始的未来锚点，已开始或完成的活动不会被回写。
- 每个生活动作使用显式动作类型、前置条件、效果和结算状态；确定性代码验证条件后才允许修改白名单状态字段，并保留动作结果与统一决策轨迹。
- 每日刷新、夜间复盘、私聊回访和闲时主动检查接入持久任务队列，支持幂等键、租约、重试上限、失败原因和启动恢复；任务载荷不包含也不会执行任意代码。

### 💞 情绪、关系与主动互动

- 新增短时、日级和关系级三层情绪，按不同半衰期衰减；关系状态必须引用已落库证据，单次变化受到小幅边界限制。
- 主动互动增加候选、考虑、等待、发送、互动、收束、冷却、中断和放弃生命周期，普通消息到来可以中断过期候选。
- 主动发送增加收益、时机、连续性、打扰和不确定性五项评分；确定性净收益未达门槛时继续观察，实际发送成功后才提交情节记忆和最终决策轨迹。
- 夜间日记采用第一人称，但只能引用真实事件、决策、反馈和回复效果记录；无法验证证据时不保存日记或关系变化。

### 🖥️ 面板、回放与升级

- 仪表盘新增时间事实、人格断言、候选反思、三层情绪、生活动作结算、决策轨迹、有证据日记和可恢复任务状态，结构化状态使用中文显示。
- 新增离线场景回放器和八类默认场景，统计决策准确率、无依据断言率、生命周期完整性、状态连续性与主动打扰风险，便于后续改动做确定性回归。
- 数据库结构升级到 v3：新增认知、任务、动作、情绪和日记表，并为旧情绪记录补齐层级、基线与衰减字段。插件重启时自动按事务执行 `v2 -> v3`，旧表和旧记录保留，失败会回滚。
- 数据库迁移只做结构增量和明确字段补齐，不从聊天摘要等自由文本反推时间事实、人格断言或日记；新认知表在产生新证据前为空是正常现象。

## 🌸 v1.0.3 · 2026-08-01

> 💗 重点补齐首次启动与自主生活执行闭环，并减少稳定生活阶段的重复模型调用。

### 🕰️ 自主生活闭环

- 首次安装时，插件启动会在后台自动补全当前业务日，不必等到第二天定时任务或第一次聊天才生成日程；生成期间仍使用防编造约束。
- 时间轴新增待进行、进行中、已完成、已跳过和已取消执行状态，并记录状态变化原因、依据和更新时间；面板会直接显示每项活动的当前执行状态。
- 后台巡检会依据权威时间轴时钟推进活动，夜间复盘再结合当天实际状态、互动和事件收束结果，不通过关键词猜测活动是否完成。
- 状态巡检在生活稳定时按基础间隔逐步退避，最长 90 分钟；到达新日程节点时会及时恢复检查，减少连续“保持不变”的模型调用。
- 穿搭判断与状态刷新解耦，只在当前/下一项日程、时段、天气温度区间、外出意愿或睡眠状态发生变化时重新调用模型；普通轮询不再重复判断同一套穿搭。
- 数据库结构升级到 v2，已有时间轴自动补为待进行状态，无需手动迁移或清空历史数据。

### 🖥️ 前台与可读性

- 补齐今日状态、世界记录、生活演化、体验层和表情口袋使用的来源、动作、状态与类型中文映射；未识别的结构化英文值统一使用中文安全回退，不再直接暴露内部标识。
- 自由叙述、文件名以及 MemOS、Grok、模型名称和接口字段等必要技术名称保留原文，不通过关键词替换改写模型内容。

## 🌸 v1.0.2 · 2026-07-31

> 💗 重点修正生图通道配置在桌面端和移动端的响应式布局，并统一新关系与记忆记录的叙述视角。

### 💞 关系与记忆

- 关系档案、会话摘要、群聊环境、消息留意、动作裁定、行为反馈和场景词等新记录，统一从当前角色自身视角生成；当前角色参与的动作、感受、关系和互动使用第一人称，其他人物仍按证据中的身份称呼。
- 聊天记忆批次现在直接复用人物称谓与叙述视角规则，根据语义主体决定视角，不通过前端正则、关键词表或固定词面替换推断主体。
- 面板保持展示模型保存的原文，不对历史自由文本做猜测式改写；升级前已有记录继续保留原文，重新生成或后续新增的记录才使用新规则。

### 🖥️ 仪表盘与配置

- 生图通道在移动端改为两层响应式布局：拖动和删除操作独立成行，接口类型与分组名称并排，地址、密钥和模型使用整行，尺寸、比例和超时稳定三列显示，避免标签、输入框和选择器重叠。
- 桌面端的图片尺寸与图片比例下拉菜单使用独立宽度并向右对齐，不再被紧凑输入框挤窄；菜单仅纵向滚动，不再出现底部横向滚动条。
- 为通道字段增加结构化字段标识，使响应式布局按字段职责排布，不依赖显示文案或字段顺序。

## 🌸 v1.0.1 · 2026-07-30

> 💗 重点修正聊天与生活事实脱节、视频素材重复准备和生图通道难以辨认的问题，并让穿搭生图与配置面板更符合连续生活场景。

### 💬 聊天表达与私聊回访

- 优化自然分段兜底：短反应或短完整句后接较长说明时，可以在强标点处分开；软停顿会结合目标长度选择更平衡的切点，减少整句不分或碎片化分段。
- 最近私聊上下文增加时间和媒体标记，并扩大可参考消息范围；旧照片、旧回复和未来约定不会再被误写成刚刚发生或已经完成。
- 私聊回访现在与闲时回复共用私聊静默门槛，未达到 `proactive_config.private_idle_minutes` 时直接等待，不提前调用模型。
- 回访生成会核对当前活动、身心状态、日程窗口和未完成约定；候选文本缺少事实依据或与当前时间线冲突时取消发送。
- 回访生成期间出现新消息，或发送前会话、日程与生活状态已经变化时，会丢弃旧回复并把普通聊天置于优先位置。
- 明确旧聊天文本不是当前事实凭证；当旧回复与结构化生活状态、真实工具结果或时间线冲突时，以可验证事实为准。

### 👗 穿搭与图片生成

- 区分“真实换装”和“只生成试穿效果图”：真实换装会先更新当前业务日的穿搭状态，再使用同一套已保存造型生图；普通创作或试穿不会改写生活状态。
- 真实换装没有产生有效的新造型、状态更新失败或与反推提示词冲突时会取消生图，避免连续重复换装或图片与面板穿搭不一致。
- 文生图和图生图通道新增自定义分组名称；图片请求、完成、尺寸异常和失败日志都会显示实际通道名称，便于区分主备接口。
- 生图接口面板改为单行紧凑布局，缩短地址、密钥、尺寸、比例和超时等控件占位，并移除横向滚动条。
- 简化当前角色与好友参考图说明，明确参考数量和建议视角。

### 🎬 视频理解

- 视频总结先准备一次视频素材，再让本地 ASR 或必剪转写复用该文件或从中提取音轨，避免同一 B 站或远程视频分别重复下载视频与音频。
- 本地准备结果无法转写时仍会回退到原始来源，保留原有容错能力。
- B 站视频缓存改用 `BV 号 + cid` 作为稳定身份，避免签名直链变化导致同一视频重复缓存。

### 🖥️ 配置与兼容性

- 现有配置和数据库无需手动迁移；旧生图通道未填写分组名称时会继续使用自动生成的通道名称。
- `proactive_config.private_idle_minutes` 现在同时约束私聊闲时回复和私聊回访，升级后回访可能比旧版本更晚触发。
- 新增并更新聊天分段、私聊回访、真实换装、视频素材复用、生图通道和仪表盘布局回归测试。

## 🌱 v1.0.0 · 2026-07-29

> 💖 首次正式发布。愿此朝夕陪伴你的生活，让角色不只是在消息里出现，而是拥有可以延续的作息、心绪、关系与片刻。

### ✨ 发布亮点

- 🕰️ 让日程、状态、睡眠、天气、穿搭和生活事件在每天的朝夕间连续演化。
- 💞 让关系记忆、随心回复、闲时续话和聊天表达共同塑造更自然的陪伴。
- 🎨 将图片、套图、合影、视频、语音、表情素材和专业视频笔记接入同一套生活上下文。
- 🌐 通过 Tavily 网页能力、Grok X 搜索、MemOS 与本地记忆补足现实信息和长期延续。
- 🖥️ 提供可视化仪表盘、配置自动保存、数据库迁移、缓存复用与后台任务生命周期管理。

### 🕰️ 自主生活

- 新增每日生活背景：主题、心情色彩、天气、状态、日程、穿搭、时间轴与生活事件会共同生成并持续演化。
- 新增业务日边界与周计划。凌晨可以自然延续前一晚，周主题和日计划作为生活判断的软参考，而不是硬性剧本。
- 新增状态、注意力与睡眠层：体力、忙碌度、社交意愿、睡眠债、无聊值、摸鱼值和观看状态会影响活动强度、邀约与回复意愿。
- 新增穿搭连续性判断：地点变化不等于立刻换衣；外出、居家、洗澡、睡前与后续计划共同决定保持、局部调整或完整换装。
- 新增日终复盘、偏好学习、生活事件与计划约定，让之后的生活从已有经验继续，而不是每天重置。

### 💞 关系与聊天

- 新增关系档案、聊天摘要、长期偏好、主观印象和人物事实边界；称呼、性别与亲疏只使用明确资料，不根据昵称或语气猜测。
- 新增随心回复、闲时回复和可选私聊回访：即时消息先裁定回应、观察或等待；会话安静后才决定是否自然续话。
- 新增聊天表达层：模型可按完整表达动作分段；失败时回退到通用换行、标点与长度规则，不依赖关键词或固定句型。
- 新增消息撤回处理：撤回的用户消息会移出当前上下文与主动回复候选，避免后续继续引用已撤回内容。
- 新增回复效果学习：只有观察到真实后续反馈时才学习表达偏好，不猜测用户态度。

### 🎨 图片、视频与语音

- 新增 GPT Image 2、Gemini 图片生成，支持文生图、图生图、角色参考图、好友参考图、生活套图与合影。
- 新增人物身份与造型归属：当前角色和好友分别维护外观、穿搭与发型，降低合影中的串脸、服装串用和身份互换。
- 新增 Grok 视频生成与图片首帧策略；新拍、续拍、合影视频和失败兜底按不同语义处理。
- 新增 B 站与普通视频理解：音频和视频素材并发准备，关键画面按变化筛选并优先多图理解。
- 新增专业视频笔记：按实际议题组织背景、论点、事实、分析、风险与建议；广告和无关片段不渲染，长内容按连续时间窗稳定合并。
- 新增 SiliconFlow 情绪语音、智能语音切换、工具状态表情和表情素材识别。

### 🌐 搜索与外部记忆

- 新增 Tavily 网页 Search、Extract、Map、Crawl 与 Research；Grok 只负责 X 平台原生搜索，网页与 X 来源明确分层。
- 新增 Quick/Deep 搜索策略、日期与域名限制、图片搜索、证据质量判断、网址定向回退和后台 Research 轮询。
- 新增搜索缓存与并发合并：相同搜索或网页读取只执行一次上游请求；缓存键隔离深度、日期、来源、域名及图片参数，失败结果不写入缓存。
- 新增 MemOS 外部长期记忆：聊天前短超时检索，超时或不可用时不阻塞主回复；可异步同步精选摘要、偏好、备忘录与纠错。

### 🖥️ 仪表盘与配置

- 新增仪表盘：查看今日生活、当前状态、时间轴、关系与世界记忆、生活演化、体验层和多模态资料。
- 新增设置分组与自动保存，可管理模型、闲时回复、搜索、MemOS、图片、视频、语音与参考图。
- 新增 QQ/OneBot 与个人微信平台适配，并为平台昵称、群聊名称和参考对象提供稳定的解析与缓存边界。

### 🛡️ 稳定性与维护

- 后台任务、外部调用与媒体生成具备生命周期保护；插件重载或终止时会取消或收束未完成工作，避免阻塞 AstrBot 主事件循环。
- 高频模型提示词统一采用稳定规则前置、动态资料后置的结构；人物、搜索、MemOS 与联系人缓存增加等价键规范化、容量限制和失败重试边界。
- 完整回归、静态检查和编译检查均已通过。
